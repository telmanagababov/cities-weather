import { CreditsComponent } from './credits.component';

describe('CreditsComponent', () => {
  it('should be defined', () => {
    // Given
    const component = new CreditsComponent();
    // When
    // Then
    expect(component).toBeTruthy();
  });
});

import { ForecastItemComponent } from './forecast-item.component';

describe('ForecastItemComponent', () => {
  it('should be defined', () => {
    // Given
    const component = new ForecastItemComponent();
    // When
    // Then
    expect(component).toBeTruthy();
  });
});
